# Eastlink 平台 MVP 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-08-14 |
| 需求来源 | 2026-08-13 Eastlink 需求会议（Irene / Zoe / 产品 IT），及 Alan 对流程图的补充批注 |
| 交付目标 | 纯前端 HTML MVP 原型（本文档将交给 Codex 实现） |
| 参考文件 | 仓库内 `hema-workflow.html`（会议纪要与工作流详情页，视觉基调参考） |

---

## 1. 背景与问题

赛龙（外贸公司，含赛龙进出口、陆景、祥峰等供应商实体）与客户 HEMA 的项目协作目前依赖线下手工：

- **全靠手搓**：下载客户 artwork 要半天；PSF 规格书逐条人工翻译成中文；产品描述、构件清单、包装清单、报价单、样品 PPT 全部手工制作；中英文两套文件反复手翻。
- **信息不协同**：HEMA 内部设计师、报价员、测试组各管各的，稿件与 PSF 经常对不上；我方也存在重复录入、多套表不一致。
- **依赖个人**：老品信息、客户语言体系全凭老员工记忆，"走一个老业务员，垮掉一个项目"；新人不知道该做什么、做到哪一步。
- **现有原型问题**：此前的 Eastlink 页面"信息太多、抓不到重点"，匹配工作台等功能与项目割裂，板块过多不简约。

**会议核心共识（六条，MVP 的设计原则）**

1. **以工作流搭平台骨架**：项目管理是核心板块，直接按 HEMA 工作流拆节点；先搭大框架，细节后补。
2. **合作模式升级**：HEMA 只给 Brief、标准与预算；设计稿 / PSF / PPA / 报价全部由赛龙落地，HEMA 只做"确认"。
3. **PSF 规格书是底层逻辑**：唯一数据源，报价单、工厂资料、下单格式全部由它派生；HEMA 按 PSF 追责，必须始终正确、可锁定最终版。
4. **节点必须可管控**：每节点有按交期倒推的 deadline、负责人、审批与提醒；卡在谁那里一目了然。
5. **先内后外的双视角**：先做全赛龙内部视角，再开 HEMA 受限视角（只见进度与名单，不见明细报价）。
6. **HEMA 即模板**：HEMA 是管控最严的客户，跑通后其他客户做减法即可复用（MVP 只做 HEMA）。

---

## 2. MVP 目标与成功标准

**目标**：交付一个可演示、可点击的纯前端原型，向 Irene / Zoe / 管理层证明"以 14 节点工作流为主轴的 Eastlink"信息架构成立，并演示关键自动化能力的交互形态。

**成功标准**

- 打开首页 10 秒内能回答：有几个项目、各自到哪个节点、谁卡住了、哪些已逾期。
- 新人视角：点开任一项目，不需要人带，能看懂"现在在哪一步、下一步做什么、deadline 是哪天、找谁"。
- 能完整走通一次演示剧本：Brief 建项目 → PSF 编辑与双语切换 → 供应商匹配 → 导出报价单 → 节点审批 → 客户视角确认 → 出运归档。
- 客户视角切换后，任何页面都看不到成本、明细报价与内部供应商池。

---

## 3. 技术与实现约束（给 Codex）

- **纯前端静态实现**：HTML + CSS + 原生 JavaScript；无后端、无构建工具、无 npm 依赖；不引用外部 CDN（需离线可打开）。
- **文件组织**：建议 `index.html` + `styles.css` + `app.js` 三件套（可直接重写仓库现有同名文件，旧原型作废）；也允许合理拆分模块文件。
- **路由**：hash 路由（如 `#/projects`、`#/projects/p1`、`#/projects/p1/node/03`、`#/suppliers`、`#/quality`、`#/clients`），刷新不丢失当前页面。
- **数据**：所有数据为内置 mock（见 §9）；用户操作（状态变更、审批、编辑）写入 LocalStorage，提供"重置演示数据"按钮。
- **时间**：mock 一个"今天"常量（建议 2026-08-14），所有逾期判断基于它，保证演示效果稳定。
- **语言**：界面中文；工作流节点、状态码等专业术语保留英文（Brief / PSF / Tender / FS / DUPRO / FRI…）。
- **响应式**：桌面优先（≥1280px 最佳），兼容平板；手机可降级但不可破版。
- **无障碍基础**：可见的 focus 状态；颜色不作为唯一状态标识（配图标或文字）。

### 3.1 视觉规范

延续 `hema-workflow.html` 的视觉基调：

| Token | 值 | 用途 |
|---|---|---|
| `--brand` | `#0567FF` | 主色：导航、主按钮、开发阶段、链接 |
| `--sky` | `#57BEEF` | 中期·产前阶段、客户（HEMA）侧标识 |
| `--mint` | `#3CD68B` | 尾期阶段、完成态、自动化成功反馈 |
| `--ink` | `#0D1E3A` | 正文 |
| `--muted` | `#51648A` | 次要文字 |
| `--bg` | `#F5F8FD` | 页面底色 |
| 语义色 | 红 `#E5484D`（逾期/驳回）、橙 `#F5A524`（待审批/风险） | 状态专用，不与主色混用 |

- 字体：中文系统字体栈（PingFang SC / Microsoft YaHei / Noto Sans CJK）；编号、货号、金额用等宽字体，`tabular-nums`。
- 卡片圆角 12–14px，浅边框 + 轻阴影；页面留白参考纪要页，避免信息过载（Irene 反复强调"高效又简洁"）。

---

## 4. 角色与视角（MVP 范围）

MVP 不做真实登录，右上角提供**视角切换器**（下拉），切换后全站生效：

| 视角 | 代号 | MVP 能做什么 | 数据红线 |
|---|---|---|---|
| 内部业务员 | `sales`（默认） | 全功能：建项目、编辑 PSF、匹配供应商、推进节点、发起审批 | 无限制 |
| 内部管理者 | `manager` | 全局看板：所有项目进度、逾期、卡点到人；只读为主 | 无限制 |
| HEMA 客户 | `client` | 只读 + 确认动作：看进度时间线、待确认清单、下载对外文件、点"确认/提出修改意见" | 见 §4.1 |
| 供应商（工厂） | `supplier` | **本期不实现**，切换器中置灰并标注"二期" | — |

### 4.1 客户视角红线（必须严格执行）

客户视角下，以下内容在任何页面**不得渲染**（不是隐藏样式，而是不输出数据）：

- 工厂成本价、内部利润、明细报价（只显示对客报价）；
- 内部供应商池与匹配过程（只显示 FS 中的最终供应商名单）；
- 内部 comments、CQV 内部评估、内部负责人分工细节；
- 供应商管理、质量管控、客户管理三个内部板块的入口（导航中不出现）。

客户可见：项目工作流进度条与节点状态、每节点"需 HEMA 确认"的事项卡片（可点击"确认"或填写修改意见）、对外文件列表（PSF 英文版、样品 PPT、FS 供应商名单）。

---

## 5. 信息架构与导航

```
顶部导航（内部视角）
├─ 项目管理（默认首页）
│   ├─ 项目总览（列表 + 汇总卡）
│   └─ 项目详情 = 14 节点工作流
│       └─ 节点详情（七要素 + 节点专属功能区）
├─ 供应商管理（平台型，常驻）
├─ 质量管控（平台型，常驻）
├─ 客户管理
└─ [右上] 客户筛选（MVP 固定 HEMA）｜视角切换｜重置演示数据
```

**明确不做的板块**（会议已砍掉，Codex 不要实现）：独立"匹配工作台"、"关键节点"、"客户决策人"、主页级"需求包拆解"。

---

## 6. 核心数据模型（mock 数据结构基准）

> 字段名供 Codex 参考，可增不可减关键字段；`*` 为列表页必显字段。

### 6.1 Project 项目

```
id, name*("BTS 2027"), client*("HEMA"), season("2027 春季"),
deliveryDate*(交期), createdDate, owner*(负责人),
status*(active|archived), currentNodeId*, skuCount*,
nodes: NodeInstance[14]（见 6.3）
```

### 6.2 SKU 产品

```
id, artNo*(货号，规则见下), name*("A5 线胶本"), category*(品类，二级，如 "本册/线圈本"),
imageEmoji/占位图, targetPrice(客户目标价), quotePrice(对客报价), factoryCost(内部成本，客户视角不输出),
matchedSupplierIds[], finalSupplierId(FS 锁定), psfId,
nodeProgress: { nodeId: done|pending }（用于节点完成度统计）
```

**货号规则**（Irene：品类编号先建档，筛编号即得品类）：`HM-<品类码>-<流水>`，如 `HM-NB-001`（NB=笔记本，BG=包袋，PN=金属笔，CU=水杯…）。品类码在 mock 中至少 4 类。

### 6.3 NodeInstance 工作流节点实例（每项目 14 个）

```
nodeId(01..14), code(Brief/Artwork/PSF/PPA/Tender/Benchmark/FS/PO/PPSample/Audit/Bulk/DUPRO/FRI/Ship),
phase(dev|pre|end), status(pending|in_progress|review|done),
deadline(由交期倒推，见 §7.2), owner(负责人), reviewer(审批人，可空),
inputs[](输入资料名), todos[]({text, done}), comments[]({author, role, time, text}),
versions[]({ver, time, author, note}), doneCount/totalCount(SKU 完成度),
clientConfirmable(bool，该节点是否需要客户确认)
```

### 6.4 PSF 规格书（核心对象）

```
id, skuId, version("V1"|"V2"|"Final"), locked(bool), updatedTime, editor,
sections: {
  productDesc:   { zh, en },                    // 产品描述
  construction:  [ {part(封面/内页/线圈…), spec_zh, spec_en} ],   // 构件清单
  surface:       [ {process(烫金/印刷/覆哑膜…), position, zh, en} ], // 表面处理
  packaging:     [ {item(吊牌/纸卡/PE袋/外箱…), spec_zh, spec_en} ], // 包材清单
  compliance:    [ {type(法规/测试/高风险点), requirement, source(QBP/PPA), critical(bool)} ]
},
history: [ {ver, time, author, change} ]
```

### 6.5 Supplier 供应商

```
id, name*("赛罗文具"), categories*[](细分品类), processes[](工艺),
moldCapability(开模), leadTimeDays*(周期), avgPriceBand(均价区间),
clients[]("HEMA","Migros"), auditStatus*(passed|pending|expired), auditExpiry,
certifications[], rating, contact
```

### 6.6 Client 客户 / ClientProduct 过往产品

```
Client: id, name("HEMA"), brandProfile{定位, 色彩, 材料偏好, 标准}, contacts[]({name, role(买手), note}),
        sopNotes[]
ClientProduct(过往产品库): id, year(2024..2026), project("BTS"), artNo, name, category,
        specSummary, imageEmoji, reusableDesc(可复用的标准描述)
```

---

## 7. 工作流详规（平台的主骨架）

### 7.1 十四节点定义表

| # | Code | 名称 | 阶段 | 赛龙动作 | HEMA 动作 | 节点产出 | 需客户确认 |
|---|---|---|---|---|---|---|---|
| 01 | Brief | 需求简报 | 开发 | 接收 Brief、建项目、SKU/货号建档、倒排时间线 | 给方向、标准、预算区间 | 已确认 Brief · SKU 清单 · 时间轴 | ✔（Brief 范围） |
| 02 | Artwork | 设计稿 | 开发 | 代做标准化设计稿 | 确认或给修改意见 | 设计稿 · 确认与修改记录 | ✔ |
| 03 | PSF | 规格书·报价单 | 开发 | 编写双语 PSF，派生报价单 | 确认 Final 版 | 双语 PSF · 报价单 · 构件/包装清单 | ✔ |
| 04 | PPA | 测试标准 | 开发 | 落地测试要求并送审（CQV 汪月明） | 给标准、确认 | 测试清单 · 高风险点提示 | ✔ |
| 05 | Tender | 一轮报价·寄样 | 开发 | 供应商匹配、报价、CQV 样品评估、寄样 | 确认样品与价格 | 匹配结果 · 分供应商报价单 · 样品记录 | ✔ |
| 06 | Benchmark | 二轮竞价 | 开发 | 按 comments 更新价格与规格 | 定款 | 定款范围 · 更新后报价 | ✔ |
| 07 | FS | 最终确认 | 开发 | 填报工厂信息、锁定权威版本 | 确认 FS、随后发 PO | Final FS · 最终供应商名单 | ✔ |
| 08 | PO | PO·包材确认 | 中期 | 提供刀模线/封模线、跟踪包材确认 | 发放 PO、交齐包材设计稿（0.5–1 月） | 正式 PO · 已确认包材稿 | ✔（包材稿） |
| 09 | PPSample | 产前样确认 | 中期 | PP Sample 打样、并行测试/MSDS | 确认产前样 | 获批 PP Sample | ✔ |
| 10 | Audit | 验厂 | 中期 | 对接 TUV 完成工厂审核（量产前） | — | 验厂通过记录 · 资质档案更新 | — |
| 11 | Bulk | 大货·签样 | 尾期 | 大货上线、大货样确认与签样 | — | 大货签样 · 生产进度 | — |
| 12 | DUPRO | 中期验货 | 尾期 | 大货完成约 10% 时中期验货 | — | DUPRO 报告 · 整改项 | — |
| 13 | FRI | 大货验货 | 尾期 | 大货完成后尾期验货、异常闭环 | — | 验货结果 · 放单依据 | — |
| 14 | Ship | 出运·归档 | 尾期 | 放单出货；归档 Final 资料并复盘入库 | — | 出运记录 · 项目归档 · 产品库沉淀 | — |

> 备注：Benchmark 在新合作模式下趋于弱化（预算前置、"三步并一步"），但节点保留，用于兼容老流程；MVP 中正常展示。

### 7.2 Deadline 倒推规则

- 建项目时输入 `deliveryDate`（交期），系统从 14 号节点向 01 号**倒推**生成每个节点的 deadline。
- 各节点默认时长（自然日，可在 mock 常量中调整）：Brief 3 / Artwork 5（对应"设计师 5 个工作日自动倒计时"）/ PSF 5 / PPA 3 / Tender 12 / Benchmark 7 / FS 5 / PO·包材 20 / PPSample 10 / Audit 7 / Bulk 30 / DUPRO 与 Bulk 并行（Bulk 开始后 +5 天）/ FRI 3 / Ship 7。
- `今天 > deadline 且 status ≠ done` → 该节点标记**已逾期**（红色），并在首页与管理者看板聚合。

### 7.3 节点状态机

```
pending（未开始）→ in_progress（进行中）→ review（待审批/待客户确认）→ done（已完成）
任意状态 + 超期 → 叠加 overdue 标识（不是独立状态）
review 被驳回 → 回到 in_progress，并要求填写驳回意见（写入 comments）
```

- 上一节点未 done 时，下一节点默认 pending 且给出提示，但**允许并行开启**（现实中多节点并行，如 Bulk 与 DUPRO）。

---

## 8. 功能需求（按优先级）

> 每条含验收标准（AC）。P0 = MVP 必须；P1 = 尽量做，做不完可用静态占位；P2 = 明确不做，界面可留"二期"入口提示。

### F1【P0】项目总览（首页）

- 顶部汇总卡：进行中项目数、总 SKU 数、待我处理数、已逾期数（4 张数据卡）。
- "我的待办"列表：聚合当前视角负责人的所有 `in_progress`/`review` 节点，按 deadline 升序，逾期置顶标红。
- 项目卡片列表：项目名、客户、交期、负责人、当前节点、迷你进度条（14 格，按阶段三色），点击进入详情。
- **AC**：首页不出现任何节点级明细表格（Irene："主页只要总览，实际操作在子层级"）；逾期项一眼可辨。

### F2【P0】项目详情 = 工作流主视图

- 页头：项目名、交期、负责人、整体进度（x/14 节点完成）、SKU 数。
- **横向流程图**：三条阶段泳道（开发蓝 / 中期浅蓝 / 尾期绿）串联 14 个节点 chip，每个 chip 显示：编号、名称、状态色点、完成度（如 `98/100`）、deadline；点击进入节点详情。视觉参考 `hema-workflow.html` 首屏流程图。
- 流程图下方：当前节点的"下一步要做什么"提示条 + 逾期节点警示条。
- **AC**：任一节点的状态、完成度、deadline 在流程图上不点击即可读到；点击 chip 路由到 F3。

### F3【P0】节点详情页（七要素）

每个节点详情页统一布局，包含七要素：

1. **deadline**（含倒计时/逾期天数）；
2. **负责人 + 审批人**（头像字母块 + 姓名）；
3. **输入资料**（文件名列表，mock 为不可下载的占位条目）；
4. **待办清单**（checkbox，可勾选，写入 LocalStorage）；
5. **审批 / comments 流**（时间线样式：作者、角色标签、时间、内容；支持在文本框追加一条）；
6. **版本记录**（V1/V2/Final 列表 + 变更说明）；
7. **SKU 完成度表**（该节点下每个 SKU 一行：货号、品名、本节点状态 done/pending 可切换；顶部显示 `已完成 x / 共 y`）。

- 状态操作按钮：开始（pending→in_progress）、提交审批（→review）、审批通过（→done）、驳回（→in_progress，必填意见）。
- `clientConfirmable` 的节点，在客户视角显示"确认 / 提出修改意见"两个按钮，操作结果写入 comments 并推进状态。
- **AC**：七要素齐全；驳回必填意见；所有操作即时反映到 F2 流程图与 F1 待办。

### F4【P0】节点专属功能区（在对应节点详情页内嵌）

- **01 Brief**：SKU 清单表（货号、品名、品类，可按货号/品类筛选——演示"筛编号出品类"）；"按交期倒推生成时间线"按钮（重算所有 deadline 并弹出确认）。
- **03 PSF**：见 F5，节点内嵌 PSF 列表入口（每 SKU 一份，显示版本与锁定状态）。
- **05 Tender**：见 F6 匹配；另含"样品照片墙"占位 + **"一键生成样品 PPT"**按钮 → 打开新视图：按客户模板渲染的样品清单页（图占位 + 货号 + 报价），提供"打印/导出 PDF"（调用 `window.print()`）。
- **07 FS**：FS 汇总表（每 SKU 一行：货号、品名、对客价、最终供应商——从匹配结果自动带入，可下拉修改）；"锁定 Final 版本"按钮（锁定后表格只读、标记 Final 徽章，解锁需填写原因写入版本记录——对应"FS 改来改去"痛点）。
- **12/13 DUPRO/FRI**：验货记录卡（检验项、结果 pass/fail、问题描述、整改状态），mock 数据体现"DUPRO 在大货 10% 时"。
- **AC**：以上五个节点打开后除七要素外能看到专属功能区，其余节点仅七要素即可。

### F5【P0】PSF 规格书中心（底层核心，做细）

入口：节点 03 内嵌 + SKU 行点击。

- **结构化编辑器**：按 §6.4 五个分区（产品描述 / 构件 / 表面处理 / 包材 / 法规测试）渲染为分组表格，单元格可编辑（中文）。
- **中英双语切换**：顶部 `中文 / EN` 切换；英文内容来自 mock 的 `en` 字段（不做真实翻译）；提供**"导出英文版"**按钮 → 新视图渲染纯英文 PSF，可 `window.print()`。
- **一键导入演示**：按钮"导入客户 PSF"→ 弹窗内置一段示例原文（模拟客户 Excel 文本）→ 点击"解析"→ 播放 1 秒加载动画 → 填充结构化表格。不做真实解析，演示交互即可，但要让观众看懂"原文 → 结构化"的映射。
- **派生生成**：按钮"生成报价单"（渲染对客报价表：货号、描述摘要、报价、包装、备注）与"生成下单资料"（渲染构件清单 + 包装清单，即"本地 AI 按公司模板生成"的演示形态，标注"由 Final 规格书自动生成"）。
- **版本与锁定**：保存生成新版本记录；"锁定为 Final"后编辑器只读；法规/测试分区中 `critical=true` 的行始终红色高亮（高风险点前置）。
- **AC**：双语切换无残缺字段；锁定后不可编辑；两个"生成"按钮产出的表格数据与 PSF 编辑内容一致（同源演示）。

### F6【P0】项目内供应商匹配（不做独立工作台）

位置：节点 05 Tender 详情页内，以及 SKU 行上的"匹配"按钮。

- 点击 SKU 的"供应商匹配"→ 侧滑面板：按该 SKU 品类自动筛出能做的供应商（如 100 家中筛出 3 家，面板顶部显示"品类：线圈本 · 命中 3 / 128 家"），每家显示：名称、周期、均价区间、验厂状态、已合作客户。
- 勾选 1–3 家加入候选 → SKU 行显示候选供应商标签；FS 阶段从候选中锁定 1 家。
- **按供应商导出报价单**：Tender 页提供按钮 → 生成按供应商分组的报价预览（A 供应商 5 款、B 供应商 20 款；多家可做的 SKU 同时出现在各家单中），可打印。
- **AC**：匹配始终发生在项目/SKU 上下文内；导出的分组与勾选结果一致。

### F7【P1】供应商管理（平台型板块）

- 供应商列表：卡片或表格，字段见 §6.5；顶部**客户筛选器**（HEMA / Migros / 全部——演示"选 HEMA 只看 HEMA 体系供应商"）+ 品类筛选 + 搜索。
- 详情侧滑：全字段 + 验厂记录（状态、有效期，过期标橙）+ 合作项目列表。
- "导入供应商"按钮：仅弹窗提示"支持批量导入（演示占位）"。
- **AC**：客户筛选生效；验厂过期有视觉提示；与 F6 匹配读取同一份数据。

### F8【P1】质量管控（平台型板块）

- 三个 Tab：**CQV 评估**（开发样品质评估表：SKU、评估项如"开口圈 ≥2.5cm / 车缝强度"、结论、评估人汪月明）、**验厂**（与 F7 验厂数据同源的汇总视图）、**验货**（DUPRO / FRI 记录汇总，与节点 12/13 同源）。
- 每条记录标注所属项目与节点，点击跳回对应节点详情（体现"质量与项目联动"）。
- **AC**：质量板块数据与节点内数据同源同步，跳转链路可用。

### F9【P1】客户管理

- HEMA 档案页：品牌定位、色彩（色块展示）、材料偏好、标准备注；联系人列表（姓名、角色如"买手"、备注）。
- **过往产品库**：按 年份 / 项目 / 品类 / 货号 搜索筛选的产品网格（emoji 或色块占位图 + 货号 + 名称 + 可复用描述）；点击弹出详情，含"复制标准描述"按钮（演示"老品词条复用"）。
- **AC**：搜索"2024 + BTS"能筛出对应 mock 产品；复制按钮真实写入剪贴板。

### F10【P0】视角切换与管理者看板

- 右上角切换 `sales / manager / client`（supplier 置灰标"二期"）。
- **manager 看板**（切到管理者后的首页）：全项目进度矩阵（行=项目，列=14 节点，色块表状态）、逾期清单（节点、负责人、逾期天数）、卡点排行（按负责人聚合的未完成数）——对应"谁卡在哪一步，不用催都知道"。
- **client 视角**：导航仅剩"我的项目"；项目页只显示进度时间线 + 待确认卡片 + 对外文件区；严格执行 §4.1 红线。
- **AC**：三种视角切换后路由与数据边界正确；用浏览器 DevTools 查看 DOM，客户视角页面中搜索不到成本/内部供应商字符串。

### F11【P2·占位】自动提醒、真实文件、外部集成

- 节点提醒仅以界面内"待办 + 逾期红点"呈现；不做推送。
- 文件上传/下载均为占位；不做真实文件处理。
- TUV / HSCC / 上海办对接、SAP、外部 AI：一律不做；相关位置可放"外链占位"图标。

---

## 9. Mock 数据要求

- **项目**：2 个 —— `BTS 2027`（进行中，当前节点 05 Tender，含 1–2 个逾期节点用于演示）与 `SIM 2026`（已走到节点 11 Bulk，含 DUPRO 记录）。
- **SKU**：BTS 2027 ≥ 10 个，覆盖 ≥4 个品类（线圈本、包袋、金属笔、水杯…），货号符合 §6.2 规则；其中 ≥2 个 SKU 完成度与整体不同（制造 `98/100` 式差异）。
- **PSF**：至少 2 份完整（一份 Final 锁定、一份 V2 编辑中），双语字段齐全，各含 ≥2 条 `critical` 法规/测试行。
- **供应商**：≥8 家，品类/周期/价格带/验厂状态有差异（≥1 家验厂过期、≥1 家待审）；分属 HEMA / Migros 体系。
- **人员**：Irene（业务负责人）、Zoe（业务）、汪月明（CQV 审批人）、Alan（管理者）、HEMA Buyer（客户联系人）。
- **过往产品库**：≥12 条，跨 2024–2026、BTS/SIM/Gift 项目。
- **comments/版本**：关键节点预置 2–3 条真实感对话（含一条客户修改意见、一条驳回记录）。

---

## 10. 页面清单与验收总表

| # | 页面/视图 | 路由 | 优先级 |
|---|---|---|---|
| 1 | 项目总览（sales 首页） | `#/projects` | P0 |
| 2 | 项目详情·工作流 | `#/projects/:id` | P0 |
| 3 | 节点详情（×14 通用模板 + 5 个专属区） | `#/projects/:id/node/:nodeId` | P0 |
| 4 | PSF 编辑器 | `#/projects/:id/psf/:skuId` | P0 |
| 5 | 报价单/样品PPT/下单资料/英文PSF 预览（可打印） | 弹层或独立视图 | P0 |
| 6 | 供应商匹配面板 | 侧滑（Tender 内） | P0 |
| 7 | 供应商管理 | `#/suppliers` | P1 |
| 8 | 质量管控 | `#/quality` | P1 |
| 9 | 客户管理 + 过往产品库 | `#/clients` | P1 |
| 10 | 管理者看板 | `#/dashboard`（manager 首页） | P0 |
| 11 | 客户视角项目页 | `#/projects/:id`（client 渲染分支） | P0 |

**最终验收清单（演示剧本）**

1. sales 视角打开首页 → 看到 4 张汇总卡与逾期待办；
2. 进入 BTS 2027 → 流程图显示 14 节点、当前在 Tender、PSF 节点 `98/100`；
3. 打开节点 03 → 打开某 SKU 的 PSF → 演示"导入客户 PSF"→ 中英切换 → 生成报价单 → 锁定 Final；
4. 回到节点 05 → 给某 SKU 匹配供应商（命中 3 家）→ 按供应商导出报价单预览 → 一键生成样品 PPT；
5. 节点 04 提交审批 → 切换视角逻辑演示驳回（填意见）→ 再通过；
6. 打开节点 07 FS → 供应商自动带入 → 锁定 Final 版本；
7. 切到 client 视角 → 只见进度与待确认卡 → 点"确认"→ 切回 sales 看到状态推进；
8. 切到 manager → 看板显示逾期与卡点排行；
9. 打开 SIM 2026 → 节点 12 看到 DUPRO 记录（大货 10%）；
10. 客户管理 → 过往产品库搜索 2024+BTS → 复制标准描述；
11. 点"重置演示数据"→ 一切恢复初始。

---

## 11. 开放问题（不阻塞 MVP，用默认值实现）

1. 各节点默认时长的业务准确值（MVP 用 §7.2 默认值，做成常量便于调整）。
2. 供应商评级维度与算法（MVP 仅展示静态星级）。
3. 客户视角可下载文件的精确范围（MVP：英文 PSF、样品 PPT、FS 名单三类）。
4. Benchmark 节点在新模式下是否默认跳过（MVP 保留，正常展示）。

---

*本 PRD 基于 2026-08-13 会议纪要整理，工作流与需求明细详见仓库 `hema-workflow.html`。*
